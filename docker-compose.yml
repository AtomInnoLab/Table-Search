version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: table-search
    ports:
      - "9991:80"
    env_file:
      - ./.env.local
    restart: unless-stopped
