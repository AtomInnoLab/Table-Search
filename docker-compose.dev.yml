services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: table-search-dev
    ports:
      - "9991:3000"
    volumes:
      - .:/app
      - /app/node_modules
    env_file:
      - ./.env.local
    restart: unless-stopped
