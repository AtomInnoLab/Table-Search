name: sp-pre-canary

on:
  pull_request_target:
    branches: [pre]
    types: [closed]
  workflow_dispatch:
    branches:
      - pre

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.base.ref || github.ref_name }}
  cancel-in-progress: true

env:
  KUBECTL_VERSION: v1.32.2
  NAMESPACE: pre
  REPO_NS: ${{ secrets.ALICLOUD_ACR_SP_NSPRE }}
  REGION_ID: ap-southeast-1
  DEPLOYMENT: table-search-pre
  CANARY_URL: https://table-search-pre.wispaper.ai

  REPO_PUB: ${{ secrets.ALICLOUD_ACR_SP_PUBLIC }}
  REPO_VPC: ${{ secrets.ALICLOUD_ACR_SP_VPC }}
  FEISHU_WEBHOOK_URL_PRE: ${{ secrets.FEISHU_WEBHOOK_URL_PRE }}
  KUBECONFIG_DEV: ${{ secrets.KUBECONFIG_DEV }}

jobs:
  build_and_push:
    if: |
      github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'pull_request_target' && github.event.pull_request.merged == true)
    name: build_and_push
    permissions:
      contents: write
    runs-on: [ubuntu-22.04]
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event.pull_request.base.ref || github.ref }}

      - name: Generate Docker Tag
        id: docker_tag
        run: |
          datetime_now=$(TZ='Asia/Shanghai' date "+%Y%m%d%H%M%S")
          echo "DOCKER_TAG=${datetime_now}" >> $GITHUB_ENV
          echo "tag=${datetime_now}" >> $GITHUB_OUTPUT

      - name: Log in to private registry (base image)
        run: |
          docker login \
            -u ${{ secrets.ALI_DOCKER_REGISTRY_USERNAME_PENGHAO }} \
            -p ${{ secrets.ALI_DOCKER_REGISTRY_PASSWD_PENGHAO }} \
            ${{ secrets.ALI_DOCKER_REGISTRY }}

      - name: Build Docker image
        run: |
          docker build -t ${{ env.REPO_PUB }}/${{ env.REPO_NS }}/${{ env.DEPLOYMENT }}:${{ env.DOCKER_TAG }} .

      - name: Log in to Aliyun ACR (Singapore)
        uses: ./.github/actions/acr-docker-login
        with:
          access-key-id: ${{ secrets.ALICLOUD_ACCESS_KEY_PH }}
          secret-access-key: ${{ secrets.ALICLOUD_SECRET_KEY_PH }}
          instance-id: ${{ secrets.ALICLOUD_ACR_INSTANCE_ID }}
          registry-url: ${{ secrets.ALICLOUD_ACR_SP_PUBLIC }}
          region-id: ${{ env.REGION_ID }}

      - name: Push Docker image
        run: |
          sudo apt-get update && sudo apt-get install -y retry
          retry -t 5 -- \
            docker push ${{ env.REPO_PUB }}/${{ env.REPO_NS }}/${{ env.DEPLOYMENT }}:${{ env.DOCKER_TAG }}

      - name: Set DOCKER_TAG for next job
        run: |
          echo "Image built: ${{ env.REPO_PUB }}/${{ env.REPO_NS }}/${{ env.DEPLOYMENT }}:${{ env.DOCKER_TAG }}"
    outputs:
      docker_tag: ${{ steps.docker_tag.outputs.tag }}

  deploy_canary:
    needs: build_and_push
    name: deploy_canary
    permissions:
      contents: write
    runs-on: [ubuntu-22.04]
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event.pull_request.base.ref || github.ref }}

      - name: Setup kubectl
        uses: ./.github/actions/setup-kubectl-ack
        with:
          kubectl-version: ${{ env.KUBECTL_VERSION }}
          kubeconfig-env: ${{ env.KUBECONFIG_DEV }}

      - name: Apply canary
        shell: bash
        run: |
          IMAGE="${{ env.REPO_VPC }}/${{ env.REPO_NS }}/${{ env.DEPLOYMENT }}:${{ needs.build_and_push.outputs.docker_tag }}"
          kubectl -n "${{ env.NAMESPACE }}" delete deployment ${{ env.DEPLOYMENT }}-canary --ignore-not-found=true
          kubectl -n "${{ env.NAMESPACE }}" set image \
            -f k8s/singapore/sp-pre-canary/deployment-canary.yml \
            ${{ env.DEPLOYMENT }}-canary="$IMAGE" \
            --local -o yaml \
          | kubectl -n "${{ env.NAMESPACE }}" apply -f - --validate=false
          kubectl -n "${{ env.NAMESPACE }}" apply -f k8s/singapore/sp-pre-canary/service-canary.yml --validate=false
          kubectl -n "${{ env.NAMESPACE }}" apply -f k8s/singapore/sp-pre-canary/ingress-canary.yaml --validate=false
          kubectl -n "${{ env.NAMESPACE }}" rollout status "deployment/${{ env.DEPLOYMENT }}-canary" --timeout=180s

      - name: Save docker_tag to artifact
        run: |
          echo "${{ needs.build_and_push.outputs.docker_tag }}" > docker_tag.txt

      - name: Upload docker_tag artifact
        uses: actions/upload-artifact@v5
        with:
          name: docker_tag
          path: docker_tag.txt
          retention-days: 7
          overwrite: true

      - name: Verify docker_tag consistency
        run: |
          EXPECTED_TAG="${{ needs.build_and_push.outputs.docker_tag }}"
          ACTUAL_TAG=$(cat docker_tag.txt | tr -d '\n\r')
          if [ "$EXPECTED_TAG" != "$ACTUAL_TAG" ]; then echo "❌ docker_tag 不一致"; exit 1; fi

      - name: Notify Feishu - canary
        uses: ./.github/actions/send-feishu-v2
        with:
          webhook: ${{ env.FEISHU_WEBHOOK_URL_PRE }}
          status-emoji: "🟡"
          title: "ACK: ${{ env.DEPLOYMENT }}-canary deployed"
          details: |
            分支: ${{ github.ref_name }}
            提交: ${{ github.sha }}
            Tag: ${{ needs.build_and_push.outputs.docker_tag }}
            测试方法：在 header 中添加 X-Canary: grey
            测试地址: ${{ env.CANARY_URL }}

            🚀 预发发布: https://github.com/${{ github.repository }}/actions/workflows/sp-pre-release.yml

  notify_docker_tag_mismatch:
    name: notify_docker_tag_mismatch
    needs: [build_and_push, deploy_canary]
    if: always() && needs.deploy_canary.result == 'failure'
    runs-on: [ubuntu-22.04]
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event.pull_request.base.ref || github.ref }}

      - name: Notify Feishu - docker_tag mismatch
        uses: ./.github/actions/send-feishu-v2
        with:
          webhook: ${{ env.FEISHU_WEBHOOK_URL_PRE }}
          status-emoji: "🔴"
          title: "ACK: ${{ env.DEPLOYMENT }}-canary docker_tag 验证失败"
          details: |
            分支: ${{ github.ref_name }}
            提交: ${{ github.sha }}
            ❌ docker_tag 验证失败！
